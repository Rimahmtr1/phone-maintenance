
        // Function to display a greeting message
        function greet(name) {
            const message = `Hello, ${name}!`; // Create the message
            document.getElementById('hh').innerText = message; // Display the message
        }
